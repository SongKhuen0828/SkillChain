# SkillChain 功能更新计划

## 📋 功能需求汇总

### 1. AI 训练数据 + Python FastAPI
- [x] 生成种子数据 (study_sessions, quiz_submissions, user_progress)
- [x] 设置 Python FastAPI 服务
- [x] 实现真正的 ML 训练 (scikit-learn)

### 2. CoursePlayer 改进
- [ ] AI Timer 自动开始 + 屏幕锁定模式
- [ ] 暂停时弹窗询问原因 (选择 + 自定义输入)
- [ ] 限制跳过未完成的 Lesson
- [ ] Video/Quiz 完成后自动继续下一个

### 3. 移除 Price
- [ ] 移除 CreateCourse 的 price 字段
- [ ] 移除 EducatorCourses 的 price 显示

### 4. Organization 重新设计
- [ ] 邀请 Learner (而非 Educator)
- [ ] Learner Settings 添加 Profile Publish
- [ ] Organization 可以看到 Published Learners
- [ ] Learner 通过 Notification 收到邀请码
- [ ] Organization 课程默认私有
- [ ] 添加 "Open to Public" 选项
- [ ] Certificate 预留 Organization Logo 位置

---

## 🔄 详细设计讨论

### CoursePlayer 智能学习流程

```
用户进入课程
    ↓
自动推荐学习方法 (Pomodoro/Flowtime/etc)
    ↓
[开始学习] → Timer 自动开始
    ↓
进入专注模式 (可选锁屏)
    ↓
如果暂停 Video:
    ├─ Timer 暂停
    ├─ 弹窗: "为什么暂停?"
    │   ├─ 需要休息
    │   ├─ 有其他事情
    │   ├─ 内容太难
    │   ├─ 自定义输入
    │   └─ 保存到 pause_reasons 表
    └─ 用于分析用户行为
    ↓
Video 播完:
    ├─ 标记 Lesson 完成
    ├─ 检查下一个内容
    │   ├─ 有 Quiz → 自动跳转 Quiz
    │   ├─ 有 Lesson → 自动播放
    │   └─ 没有 → 显示模块完成
    └─ 弹窗确认 "继续学习?" (可选)
```

**问题:**
1. 锁屏模式是指什么? 全屏? 禁止切换标签?
2. 自动继续前需要用户确认吗? 还是直接播放?

---

### Organization 新流程

```
当前流程 (邀请 Educator):
Organization → 生成邀请码 → Educator 注册时使用 → 加入组织

新流程 (邀请 Learner):
1. Learner 设置 Profile 为 Public
2. Organization 搜索 Public Learners
3. Organization 发送邀请 (Notification)
4. Learner 收到邀请码
5. Learner 输入邀请码加入组织
6. 加入后:
   - 可以看到组织的私有课程
   - 完成课程获得带组织 Logo 的证书
```

**数据库变更:**
```sql
-- profiles 表添加
profile_public BOOLEAN DEFAULT FALSE

-- org_invite_codes 修改
-- 移除 target_role 或改为 'learner'

-- courses 表添加
visibility TEXT DEFAULT 'private' -- 'private' | 'public' | 'org_only'

-- notifications 表 (已存在)
-- type: 'org_invitation'
-- data: { org_id, org_name, invite_code }
```

**问题:**
1. Learner 可以同时属于多个 Organization 吗?
2. 如果 Learner 离开 Organization, 之前的证书怎么处理?

---

### Certificate Logo 设计

```
┌─────────────────────────────────────────────────┐
│                                    [Org Logo]   │ ← 右上角
│                                                 │
│           CERTIFICATE OF COMPLETION             │
│                                                 │
│                 [Student Name]                  │
│                                                 │
│           has successfully completed            │
│                                                 │
│              [Course Title]                     │
│                                                 │
│  Date: [Date]        [Signature]                │
│                                                 │
│  [SkillChain Logo]                             │
└─────────────────────────────────────────────────┘
```

---

## 📝 请确认以下问题

1. **CoursePlayer 锁屏**: 是全屏模式还是禁止切换标签?
2. **自动继续**: 需要弹窗确认还是直接播放?
3. **多组织**: Learner 可以加入多个 Organization 吗?
4. **证书 Logo**: Organization Logo 放在右上角可以吗?
5. **暂停原因**: 需要中文还是英文选项?

确认后我会开始实现！

