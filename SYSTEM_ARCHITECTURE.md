# SkillChain 系统架构详解

## 📌 总体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React + Vite)                      │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │   Learner   │ │  Educator   │ │  Org Admin  │ │System Admin │   │
│  │  Dashboard  │ │  Dashboard  │ │  Dashboard  │ │  Dashboard  │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    TensorFlow.js (Client-side AI)            │   │
│  │         AdaptiveSchedulingEngine - 客户端实时训练模型          │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     SUPABASE (Backend-as-a-Service)                 │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │ PostgreSQL  │ │    Auth     │ │   Storage   │ │ Edge Funcs  │   │
│  │  Database   │ │   Service   │ │   Bucket    │ │  (Deno)     │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
│        │                                               │            │
│        │              ai-companion ────────────────────┤            │
│        │              train-ai-models ─────────────────┤            │
│        │              grade-quiz ─────────────────────────┤         │
└─────────────────────────────────────────────────────────────────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          ▼                       ▼                       ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│   Groq API      │   │  Pinata/IPFS    │   │ Polygon Amoy    │
│   (LLM Chat)    │   │  (NFT Storage)  │   │  (Blockchain)   │
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 🤖 AI 功能详解

### 1. TensorFlow.js (客户端 AI)

**文件**: `apps/web/src/lib/ai/AdaptiveSchedulingEngine.ts`

**工作原理**:
```
用户完成 Study Session → 数据保存到 study_sessions 表
                              ↓
           AdaptiveSchedulingEngine 获取最近500条数据
                              ↓
                    特征工程 (Feature Engineering)
                    - 时间 (hour/24 归一化)
                    - 学习方法 (pomodoro/flowtime/blitz/52_17)
                              ↓
                    构建神经网络 (Dense 8 + Dense 1)
                              ↓
                    训练 30 epochs (Binary Classification)
                              ↓
                    保存模型到 localStorage
                              ↓
                    预测最佳学习方法和时间
```

**训练数据来源**:
- **study_sessions 表**: 每次用户使用 FocusTimer 完成学习
  - `started_at`: 开始时间
  - `method_used`: 使用的学习方法
  - `completed`: 是否完成
  - `duration_seconds`: 学习时长
  - `tab_switch_count`: 切换标签次数

**实际使用场景**:
1. FocusTimer 组件启动时调用 `predictBestMethod()`
2. 返回推荐的学习方法 (pomodoro/flowtime 等)
3. 根据用户的历史数据个性化推荐

**代码示例** (FocusTimer.tsx):
```typescript
// 获取 AI 推荐
const { method, confidence } = await schedulingEngine.predictBestMethod(user.id);
setRecommendedMethod(method);
setAiConfidence(confidence);
```

---

### 2. Groq API (文本生成 AI)

**文件**: 
- `apps/web/src/lib/ai/AICompanionService.ts`
- `supabase/functions/ai-companion/index.ts`

**用途**: 生成个性化文字内容，不是训练模型

**使用场景**:
1. Dashboard 问候语
2. 学习鼓励消息
3. 课程推荐解释
4. Schedule 调整说明

**流程**:
```
前端发送 prompt + context → Edge Function → Groq API
                                              ↓
                              llama-3.3-70b-versatile 模型
                                              ↓
                              返回生成的文字 → 显示在 UI
```

**注意**: Groq 只是调用现成的 LLM，不涉及训练

---

### 3. 服务器端训练 (Edge Function)

**文件**: `supabase/functions/train-ai-models/index.ts`

**训练的模型**:
1. **Scheduling Model**: 预测最佳学习时间和方法
2. **Recommendation Model**: 课程推荐
3. **Performance Model**: 学习效果预测

**训练数据**:
- study_sessions (学习会话)
- quiz_submissions (测验结果)

**训练指标**:
- Accuracy (准确率)
- Precision (精确率)
- Recall (召回率)
- F1 Score

**当前状态**: 
- ⚠️ 使用简化的统计算法，不是真正的 ML
- ✅ 可以扩展为 Python FastAPI 后端

---

### 4. Python FastAPI (可选扩展)

**目录**: `packages/ai-engine/`

**当前状态**: 基础框架存在，但未完成

**可以用来做**:
1. 更复杂的 ML 模型 (scikit-learn, PyTorch)
2. 模型持久化存储
3. 批量训练任务
4. 定时重训练

---

## ⛓️ Blockchain 功能详解

### 整体流程

```
课程完成 100% → checkCourseCompletion()
                      ↓
            生成证书图像 (html2canvas)
                      ↓
            上传图像到 IPFS (Pinata)
                      ↓
            创建 NFT 元数据 JSON
                      ↓
            上传元数据到 IPFS (Pinata)
                      ↓
            调用智能合约 mintCertificate()
                      ↓
            保存到数据库 (certificates 表)
```

### 1. Pinata (IPFS 存储)

**是什么**: 
- IPFS = InterPlanetary File System (星际文件系统)
- 去中心化的文件存储网络
- Pinata = IPFS 的托管服务 (像 AWS S3 for IPFS)

**为什么需要**:
- NFT 需要存储图片和元数据
- 区块链上存储数据太贵
- IPFS 提供永久、去中心化存储
- 使用 CID (Content ID) 作为唯一标识

**文件**: `apps/web/src/lib/ipfs/pinata.ts`

**流程**:
```
证书图像 (PNG) → Pinata API → IPFS 网络 → 返回 CID (e.g., QmXxx...)
                                              ↓
                                    ipfs://QmXxx... (URI)
```

**需要配置**:
```env
VITE_PINATA_API_KEY=xxx
VITE_PINATA_SECRET_KEY=xxx
```

**获取方式**:
1. 访问 https://app.pinata.cloud
2. 注册免费账号
3. 创建 API Key

---

### 2. 智能合约

**文件**: `packages/contracts/contracts/CertificateNFT.sol`

**功能**:
- ERC-721 NFT 标准
- `mintCertificate()`: 铸造证书 NFT
- 不可转让 (Soulbound)

**部署状态**: 需要部署到 Polygon Amoy 测试网

**需要配置**:
```env
VITE_CERTIFICATE_CONTRACT_ADDRESS=0x... (部署后的合约地址)
VITE_ADMIN_PRIVATE_KEY=xxx (用于签名交易的私钥)
VITE_POLYGON_RPC_URL=https://rpc-amoy.polygon.technology
```

---

### 3. 模式切换

**文件**: `apps/web/src/lib/blockchain/BlockchainService.ts`

**Simulation 模式** (开发/测试):
```env
VITE_USE_SIMULATION=true
```
- 不需要真实区块链
- 生成假的 tx_hash
- 不需要 Gas 费

**Real 模式** (生产):
```env
VITE_USE_SIMULATION=false
VITE_CERTIFICATE_CONTRACT_ADDRESS=0x...
VITE_ADMIN_PRIVATE_KEY=0x...
```
- 真实区块链交易
- 需要 MATIC 代币支付 Gas
- 证书永久记录在链上

---

## 📊 数据流总结

### AI 训练数据流

```
用户行为 → Supabase DB → AI 模型训练 → 预测结果

具体来源:
├── study_sessions (学习会话)
│   └── 每次 FocusTimer 完成时记录
├── quiz_submissions (测验提交)
│   └── 每次测验完成时记录
├── user_progress (学习进度)
│   └── 每次 Lesson 完成时记录
└── ai_preferences (用户偏好)
    └── Onboarding 问卷结果
```

### 证书铸造数据流

```
课程完成 → 生成证书 → IPFS 上传 → 区块链铸造 → 数据库保存

涉及表:
├── enrollments (报名状态: completed)
├── certificates (证书记录)
│   ├── tx_hash (交易哈希)
│   ├── token_id (NFT Token ID)
│   └── ipfs_hash (IPFS CID)
└── profiles.wallet_address (用户钱包)
```

---

## 🚀 你需要做什么

### 1. 基础配置 (必须)

在 `.env` 文件添加:

```env
# Supabase (已有)
VITE_SUPABASE_URL=xxx
VITE_SUPABASE_ANON_KEY=xxx

# Groq (AI Companion)
VITE_GROQ_API_KEY=xxx
# 获取: https://console.groq.com

# Pinata (IPFS)
VITE_PINATA_API_KEY=xxx
VITE_PINATA_SECRET_KEY=xxx
# 获取: https://app.pinata.cloud

# Blockchain (可选，使用 Simulation 模式可跳过)
VITE_USE_SIMULATION=true  # 开发时设为 true
# VITE_CERTIFICATE_CONTRACT_ADDRESS=0x...  # 生产时填写
# VITE_ADMIN_PRIVATE_KEY=0x...  # 生产时填写
```

### 2. 数据库迁移 (必须)

在 Supabase SQL Editor 执行:
1. `supabase/migrations/create_notifications_table.sql`
2. `supabase/migrations/create_ai_model_metrics.sql`

### 3. Edge Functions 部署 (可选)

```bash
supabase functions deploy ai-companion
supabase functions deploy train-ai-models
```

### 4. 智能合约部署 (可选，FYP 演示用 Simulation 即可)

```bash
cd packages/contracts
npx hardhat compile
npx hardhat run scripts/deploy.ts --network amoy
```

---

## ❓ 常见问题

### Q: AI 是真的在训练吗？
A: 是的！TensorFlow.js 在浏览器中运行真正的神经网络训练。训练数据来自 study_sessions 表。

### Q: 训练数据从哪来？
A: 用户使用 FocusTimer 学习时，每次会话结束会保存到 study_sessions 表。

### Q: Groq 是训练还是推理？
A: 只是推理 (Inference)。调用现成的 llama-3.3 模型生成文字，不涉及训练。

### Q: 为什么需要 Pinata？
A: NFT 需要存储图片和元数据，区块链上存储太贵，所以用 IPFS 去中心化存储。

### Q: Simulation 模式能用于 FYP 吗？
A: 可以！Simulation 模式生成的数据看起来完全真实，适合演示。

### Q: 是否可以用 Python FastAPI？
A: 可以！`packages/ai-engine/` 已有基础框架，可以扩展为:
   - scikit-learn 模型
   - PyTorch 深度学习
   - 定时训练任务

